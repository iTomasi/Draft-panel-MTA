<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%- include ("templates/head.ejs") %>
    <link href="/css/index.css" rel="stylesheet"/>
</head>
<body>
    <%- include ("templates/header.ejs") %>
    <div class="content">
        <div class="table" style="<%= data[0] === undefined ? 'display: none' : 'display: block'%>">
            <div class="table__info" style="display: flex; text-align: center; background: #1f2833; padding: 10px 0;">
                <span style="width: 30%;">Nickname</span>
                <span style="width: 30%;">Points</span>
                <span style="width: 20%;">
                    <i class="i__crown fas fa-crown"></i>
                </span>
                <span style="width: 20%;">
                    <i class="i__bill fas fa-money-bill"></i>
                </span>
            </div>

            <div class="table__players">
                <% for( let i = 0; i < data.length; i++ ) { %>

                    <% if (data[i].totalKrowns > 0 || data[i].totalCash > 0) { %>
                        <div class="player">
                            <span class="player__nickname"><%= data[i].nickname %></span>
                            <span class="player__points"><%= data[i].points %></span>
                            <span class="player__krowns">
                                <i class="i__crown fas fa-crown"></i>
                                <%= data[i].totalKrowns %>
                            </span>
                            <span class="player__money">
                                <i class="i__bill fas fa-money-bill"></i>
                                <%= data[i].totalCash %>
                            </span>
                        </div>
                    <% } %>
                    
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>